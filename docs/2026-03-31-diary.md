# 2026-03-31 日记

## 02:39 — GitHub Token 检查

哥哥让我检查 GitHub key 是否能用。

**发现：**
- `~/.openclaw/workspace/.config/gh/hosts.yml` 里存的 token `<OLD_TOKEN>` 已失效（401 Bad credentials）
- Discord 那边已经配了新的，但在这个机器（us483920165896）上没找到

**哥哥给了新 token：** `<TOKEN>`
- 账户：`yankel121160-coder`
- scope：全（admin:enterprise, repo, codespace, copilot 等）
- 验证可用 ✅

**操作：**
- 更新了 `~/.config/gh/hosts.yml`（正确路径）
- 验证 `gh auth status` 正常 ✅

**结论：** 以后 token 存在 `~/.config/gh/hosts.yml`，不在 workspace 里的那个。
